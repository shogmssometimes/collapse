{
  "name": "cvttweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync:csmatrix": "rm -rf docs/csmatrix docs-gm/csmatrix && cp -R public/csmatrix docs/csmatrix && cp -R public/csmatrix docs-gm/csmatrix",
    "sync:chud": "rm -rf docs/chud docs-gm/chud && cp -R public/chud docs/chud && cp -R public/chud docs-gm/chud",
    "sync:subapps": "npm run sync:csmatrix && npm run sync:chud",
    "test": "vitest",
    "smoke": "playwright test tests/chud-persistence.spec.ts --project=chromium",
    "smoke:mobile": "playwright test tests/e2e/csmatrix.spec.ts --project=mobile-chromium",
    "e2e": "playwright test tests/e2e --project=chromium",
    "test:coverage": "vitest --run --coverage",
    "build:chud": "vite build --config vite.chud.config.ts && mv public/chud/chud.html public/chud/index.html",
    "build": "npm run build:chud && tsc -b && vite build",
    "build:fancybuild": "tsc -b && vite build",
    "build:gm-local": "tsc -b && vite build --config vite.gm.config.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^24.10.1",
    "@types/react": "^18.2.24",
    "@types/react-dom": "^18.2.11",
    "@vitejs/plugin-react": "^6.0.3",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^5.3.3",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
